interface TickProps {
    tickHeight: number;
    text: string;
    margin?: number;
    textSize?: number;
    strokeWidth?: number;
    strokeColor?: string;
    textColor?: string;
    font?: string;
}
export default function Tick({ tickHeight, text, margin, textSize, strokeWidth, strokeColor, textColor, font, }: TickProps): import("react/jsx-runtime").JSX.Element;
export {};
